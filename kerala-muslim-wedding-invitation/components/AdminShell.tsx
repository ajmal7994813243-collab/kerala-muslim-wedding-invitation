'use client';import {useEffect,useState} from 'react';import {onAuthStateChanged,signOut,User} from 'firebase/auth';import {auth} from '@/lib/firebase-client';import {useRouter} from 'next/navigation';
export default function AdminShell({children}:{children:React.ReactNode}){const [user,setUser]=useState<User|null|undefined>();const router=useRouter();useEffect(()=>onAuthStateChanged(auth,u=>{setUser(u);if(!u)router.replace('/admin/login')}),[router]);if(user===undefined||!user)return <div className="min-h-screen grid place-items-center">Checking admin access…</div>;return <div className="min-h-screen"><header className="sticky top-0 z-20 border-b bg-white/95 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><a href="/admin" className="font-serif text-xl text-wine">Wedding Admin</a><button onClick={()=>signOut(auth)} className="rounded-full border px-4 py-2 text-sm">Logout</button></div></header>{children}</div>}
