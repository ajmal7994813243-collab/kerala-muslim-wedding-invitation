import {NextResponse} from 'next/server';import {adminDb} from '@/lib/firebase-admin';export async function POST(req:Request){try{const {token}=await req.json();const s=await adminDb().collection('invitations').where('token','==',token).limit(1).get();if(s.empty)return NextResponse.json({ok:false},{status:404});const d=s.docs[0],x=d.data();await d.ref.update({openCount:(x.openCount||0)+1,lastOpenedAt:new Date()});return NextResponse.json({ok:true})}catch{return NextResponse.json({ok:false},{status:400})}}
