'use client';import {useState} from 'react';import {signInWithEmailAndPassword} from 'firebase/auth';import {auth} from '@/lib/firebase-client';import {useRouter} from 'next/navigation';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setError('');try{await signInWithEmailAndPassword(auth,email,password);router.push('/admin')}catch(err:any){setError(err.message||'Login failed')}}return <main className="min-h-screen grid place-items-center p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 card-shadow"><div className="arabic text-center text-3xl">السلام عليكم</div><h1 className="mt-4 text-3xl font-serif text-wine text-center">Admin Login</h1><input required type="email" placeholder="Admin email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-8 w-full rounded-xl border p-3"/><input required type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-3 w-full rounded-xl border p-3"/><button className="mt-5 w-full rounded-xl bg-wine p-3 text-white">Sign in</button>{error&&<p className="mt-4 text-sm text-red-700">{error}</p>}</form></main>}
